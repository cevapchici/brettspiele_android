<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.munich.iubh.brettspiele">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".games.VierGewinnt"
            android:label="@string/title_activity_muehle"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".games.SchiffeVersenken"
            android:label="@string/title_activity_schiffe_versenken"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".games.TicTacToe.TicTacToe"
            android:label="@string/title_activity_tic_tac_toe"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>